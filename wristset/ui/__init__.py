"""Streamlit annotation + demo UI (§12).

The app itself lives in ``app.py`` and is launched with ``streamlit run
wristset/ui/app.py``. This package is import-safe without Streamlit installed so the
rest of the library (and the test suite) never depends on the ``ui`` extra.
"""
